# typesafe-sdk-rust-macros

`#[derive(QuestionSet)]` for [typesafe-sdk-rust](https://crates.io/crates/typesafe-sdk-rust).
Use it through that crate, as `typesafe_sdk::QuestionSet`; this package is released with it.
